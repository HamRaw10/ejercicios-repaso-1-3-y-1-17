#include <stdio.h>
#include <stdlib.h>

int main(int argc, char * argv[]) {
 	float invertir;
 	scanf("%f", &invertir);
	 printf("El sueldo que obtendra al final es: %.2f", invertir*1.2); 

return 0;
}
